/**
 * Build-time globals injected by the host app's bundler (Vite define).
 * The UI package references these but each app is responsible for providing them.
 */
declare const __APP_VERSION__: string;
