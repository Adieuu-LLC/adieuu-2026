/**
 * Default maximum video duration (seconds) when no platform or account override applies.
 * Keep in sync with product default (currently 5 minutes).
 */
export const DEFAULT_MAX_VIDEO_DURATION_SECONDS = 300;
